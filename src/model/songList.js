class SongList {
  constructor({
    author,
    desc,
    image,
    id,
  }) {
    this.author = author;
    this.desc = desc;
    this.image = image;
    this.id = id;
  }
}

export default SongList;
